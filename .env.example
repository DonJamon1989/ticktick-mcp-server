# TickTick OAuth credentials
#
# Copy this file to `.env` and fill in the values below with your own
# client information. See the TickTick developer documentation for details.

OAUTH_CLIENT_ID=your_client_id_here
OAUTH_CLIENT_SECRET=your_client_secret_here

# Authorization endpoint for TickTick
OAUTH_AUTH_URL=https://ticktick.com/oauth/authorize

# Token exchange endpoint for TickTick
OAUTH_TOKEN_URL=https://ticktick.com/oauth/token

# Space separated list of OAuth scopes
OAUTH_SCOPE=tasks:read tasks:write

# Redirect URI registered with TickTick. For hosted deployments this should be
# your public URL followed by `/oauth/callback`, e.g. https://example.com/oauth/callback
OAUTH_REDIRECT=https://example.com/oauth/callback
